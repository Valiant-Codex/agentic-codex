---
type: memory
title: <AGENT> Distilled Memory
description: Compact, high-signal durable memory for <AGENT>, distilled from the runtime working tier.
tags: [<AGENT>, memory]
status: active
timestamp: 2026-01-01T00:00:00Z
---
# <AGENT> — Distilled Memory

Compact durable memory. Add a short section per durable fact — a decision and its reasoning, a
constraint, a hard-won operational lesson. Prefer few, well-written entries over many fragments; this
file is inlined into `CLAUDE.md` by an `@`-import if you choose to wire one, so length is a real cost.

Promote a fact here when it is a standing decision a fresh session would otherwise re-litigate. Everything
episodic stays in the runtime's own auto-memory, mirrored nightly into `auto/`. Never
store secrets. The `agent-audit` skill is the periodic pass where you and <OWNER> curate this file.

*(Empty to start — write your first entry when something is worth keeping.)*
